#!/usr/bin/env bash
#
# Install all skills from this repo into a project's .claude/skills/ directory
# by copying each skill directory.
#
# Usage: install-claude-skills-project <project-dir>
#
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
SKILLS_SRC="$SCRIPT_DIR/../skills"

if [[ $# -lt 1 ]]; then
    echo "Usage: $(basename "$0") <project-dir>" >&2
    echo "" >&2
    echo "Copies skills into <project-dir>/.claude/skills/" >&2
    exit 1
fi

PROJECT_DIR="$1"

if [[ ! -d "$PROJECT_DIR" ]]; then
    echo "Error: project directory not found: $PROJECT_DIR" >&2
    exit 1
fi

if [[ ! -d "$SKILLS_SRC" ]]; then
    echo "Error: skills directory not found at $SKILLS_SRC" >&2
    exit 1
fi

SKILLS_DST="$PROJECT_DIR/.claude/skills"
mkdir -p "$SKILLS_DST"

installed=()
for skill_dir in "$SKILLS_SRC"/*/; do
    [[ -d "$skill_dir" ]] || continue
    name="$(basename "$skill_dir")"
    target="$SKILLS_DST/$name"

    if [[ -d "$target" ]] && diff -rq "$skill_dir" "$target" >/dev/null 2>&1; then
        echo "  ok  $name"
        installed+=("$name")
        continue
    fi

    rm -rf "$target"
    cp -r "$skill_dir" "$target"
    echo "  added  $name -> $target"
    installed+=("$name")
done

echo ""
echo "Installed ${#installed[@]} skill(s) to $SKILLS_DST"
